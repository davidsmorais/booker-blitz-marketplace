# Output Format Preferences

- Prefers prompt/id mapping files in `<id>:<prompt>` format, one entry per line, with no extra text in the file. Confidence: 0.9
- Prefers generating dedicated prompt files (e.g., `prompts.md`, `staff_prompts.md`) to drive later image generation. Confidence: 0.7
