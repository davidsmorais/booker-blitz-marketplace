{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/davidsmorais/booker-blitz-marketplace/raw/main/schema/bbtheme.schema.json",
  "title": "Booker Blitz theme (.bbtheme)",
  "description": "Matches game format bbtheme version 1 (src/types/bbtheme.ts). Background slot names stay fixed; the data URL MIME may be png, jpeg, or webp.",
  "type": "object",
  "additionalProperties": false,
  "required": ["format", "version", "name", "colors"],
  "properties": {
    "format": { "const": "bbtheme" },
    "version": { "const": 1 },
    "name": { "type": "string", "minLength": 1 },
    "colors": {
      "type": "object",
      "additionalProperties": false,
      "required": ["primary", "secondary", "accent", "hueRotation"],
      "properties": {
        "primary": { "$ref": "#/$defs/hexColor" },
        "secondary": { "$ref": "#/$defs/hexColor" },
        "accent": { "$ref": "#/$defs/hexColor" },
        "hueRotation": { "type": "number", "minimum": 0, "maximum": 360 },
        "mode": { "enum": ["light", "dark"] },
        "success": { "$ref": "#/$defs/hexColor" },
        "warning": { "$ref": "#/$defs/hexColor" },
        "money": { "$ref": "#/$defs/hexColor" },
        "statGreat": { "$ref": "#/$defs/hexColor" },
        "statGood": { "$ref": "#/$defs/hexColor" },
        "statAvg": { "$ref": "#/$defs/hexColor" },
        "statLow": { "$ref": "#/$defs/hexColor" },
        "statBad": { "$ref": "#/$defs/hexColor" }
      }
    },
    "backgrounds": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "wp/1.png": { "$ref": "#/$defs/dataImage" },
        "wp/2.png": { "$ref": "#/$defs/dataImage" },
        "wp/3.png": { "$ref": "#/$defs/dataImage" },
        "wp/4.png": { "$ref": "#/$defs/dataImage" },
        "wp/5.png": { "$ref": "#/$defs/dataImage" },
        "wp/contract-table.png": { "$ref": "#/$defs/dataImage" }
      }
    }
  },
  "$defs": {
    "hexColor": { "type": "string", "pattern": "^#[0-9a-fA-F]{6}$" },
    "dataImage": { "type": "string", "pattern": "^data:image/.+;base64," }
  }
}
